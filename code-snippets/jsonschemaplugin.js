require([
    "amd-plugins/jsonschema!application/schema/services/TutorialService"
], function (
    TutorialService
) {
    // Tutorial Service is loaded and fully resolved (all references have been loaded as well

});